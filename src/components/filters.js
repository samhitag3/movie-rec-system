import './filters.css'

export default function Filters() {
    return (
        <div id='filter-block'>
           <p>type, genre, language, with-cast, rating, release date, country</p>
        </div>
    );
}